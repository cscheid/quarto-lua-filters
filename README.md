# A quarto port of `pandoc/lua-filters`

This is a repo for testing the lua filters in `pandoc/lua-filters` under upcoming changes in quarto's filters.

**This is not meant as documentation for quarto features!**
